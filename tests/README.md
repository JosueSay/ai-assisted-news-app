# Pruebas entre servicios

Contiene validaciones que requieren el proyecto completo o varios servicios. No debe duplicar las
pruebas unitarias mantenidas dentro de `backend/` y `frontend/`.

- `integration/`: integración real entre servicios e infraestructura.
- `e2e/`: recorridos completos desde la interfaz hasta la persistencia.
