# Código fuente del frontend

Directorio reservado para el punto de entrada, navegación, estilos globales y composición general de
la aplicación frontend.

La lógica de producto debe agruparse por funcionalidad en `features/`; los elementos compartidos en
`components/`; y la comunicación con APIs en `services/`.
